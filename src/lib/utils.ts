// A handful of shadcn-generated components (calendar, chart, input-otp, menubar,
// pagination, resizable) import "cn" from this path by convention. The actual
// implementation lives in shared/lib/utils.ts alongside the rest of this app's
// shared code - this just re-exports it so those components resolve correctly.
export { cn } from "@/shared/lib/utils";
